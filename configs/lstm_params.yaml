model:
  hidden_size: 128
  num_layers: 2
  dropout: 0.2
  sequence_length: 30  # lookback window in days
  bidirectional: false

training:
  batch_size: 32
  epochs: 100
  learning_rate: 0.001
  weight_decay: 0.0001
  patience: 15  # early stopping
  test_size: 0.2
  random_state: 42
